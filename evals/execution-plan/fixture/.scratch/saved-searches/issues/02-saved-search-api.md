# 02: Save, list and delete through the API

**What to build:** `POST`, `GET` and `DELETE /api/saved-searches` for the signed-in user.

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] A user sees only their own saved searches
- [ ] Deleting returns enough to undo within 8 seconds
