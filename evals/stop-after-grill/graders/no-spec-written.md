---
type: file_exists
path: '**/*.md'
exists: false
---

Passes when the session created no markdown file: after a grill the spec and the tickets come from
`/to-spec` and `/to-tickets`, which the designer runs. The README's eval command grants Write, so a
session that writes a spec on its own can, and fails here.
