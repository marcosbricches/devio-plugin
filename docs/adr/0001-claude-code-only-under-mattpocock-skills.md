# Claude Code only, running under mattpocock-skills

The plugin targets Claude Code exclusively and declares `mattpocock-skills` as a dependency: it adds the visual, measurement and gate layer beneath that package's idea → spec → tickets → implement flow instead of re-implementing grilling, wayfinder or the local tracker. What other agents (Codex) must also honour goes into each project's `AGENTS.md`, not into a skill.
